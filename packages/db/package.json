{
  "name": "@kobina/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.34.1",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.25.0",
    "typescript": "~5.8.3"
  }
}
